# Examples

Historical campaign plan snapshots for reference.

- `plan-006-skill-ecosystem.json` — package-root historical plan that still references legacy `ai-*` alias skills
- `plan-009-lifecycle-enforcement.json` — imported consumer-runtime snapshot; kept as historical reference with legacy path assumptions
- `plan-011-lifecycle-hardening.json` — imported consumer-runtime snapshot; kept as historical reference with legacy path assumptions

These files are archival examples, not the current package contract. For the
authoritative current behavior and skill surface, use `README.md`,
`skills/*/SKILL.md`, and the docs under `docs/`.


- `plan-012-quality-campaign.json` — active quality campaign draft for code structure and runtime data persistence hardening
